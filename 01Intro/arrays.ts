const HeroName:string[] = []
const HeroPower:number[]=[]
{/*
even we can define like this 

const HeroName : Array<string>=[]
const HeroPower:Array<number>=[]
 */}


HeroName.push("hulk")
HeroPower.push(1)


type User ={
    name:string
    mobile:number
}

const CreateUser:User[]=[]
CreateUser.push({name:"",mobile:1})


//an array inside array
const HeroineNumber : number[][]=[
    [111]
]
HeroineNumber.push([222])

